# 完整事件正文实施计划

1. 在 schema 测试中将旧的“客户端截到 160”断言替换为“230 原样保留、231 拒绝”。
2. 在提示词测试中把目标更新为 80–180，并保留完整句子要求。
3. 增加生成完成页的顺序回归测试，保证确认按钮在正文之后。
4. 将运行时与存档 schema 上限改为按 Unicode 字符计数的 230，复用完整句语法校验，并移除 `trimNarrative` 及调用点。
5. 将 DeepSeek 静态协议中的正文目标改为 80–180。
6. 明确生成页的纵向滚动与普通文档流，并给事件页内容层增加溢出滚动，避免正文、行动或底部按钮被挤压、覆盖。
7. 更新 `AGENTS.md` 与 `PROJECT_CONTEXT.md`，运行自动化、构建和一次真实体验。
8. 复审差异，提交并推送 `main`。
