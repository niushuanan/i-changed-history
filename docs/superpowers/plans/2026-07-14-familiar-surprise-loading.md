# Familiar Surprise And Loading Room Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Keep counterfactual history surprising without forced geographic jumps, and turn AI wait time into a polished pale-gray archival reveal.

**Architecture:** Strengthen the continuation prompt with a two-dimension novelty rule and Chinese-player familiarity guidance. Keep the timeline state machine unchanged. Replace the static loading screen with a stage-aware component that selects one of three generated raster backgrounds and cycles through deterministic causal-processing statuses.

**Tech Stack:** React 19, TypeScript, Vite, Vitest, DeepSeek V4 Flash, ImageGen WebP assets.

## Tasks

- [x] Add failing prompt tests for no forced cross-continent jump, familiar Chinese anchors, and two-dimension novelty.
- [x] Add failing component tests for early, relay, and 2026 loading artwork.
- [x] Update prompt and fallback relay language.
- [x] Convert and install the three generated images.
- [x] Implement the stage-aware pale-gray loading room and responsive styles.
- [x] Run full tests, typecheck, build, browser QA, commit, and push.
